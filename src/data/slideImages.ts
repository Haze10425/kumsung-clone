export const slideImages = {
  line01: Array.from({ length: 16 }, (_, i) => ({
    src: `./images/slides/line01-${i + 1}.png`,
    alt: `Line01 이미지 ${i + 1}`,
  })),
  line02: Array.from({ length: 15 }, (_, i) => ({
    src: `./images/slides/line02-${i + 1}.png`,
    alt: `Line02 이미지 ${i + 1}`,
  })),
  line03: Array.from({ length: 16 }, (_, i) => ({
    src: `./images/slides/line03-${i + 1}.png`,
    alt: `Line03 이미지 ${i + 1}`,
  })),
  line04: Array.from({ length: 16 }, (_, i) => ({
    src: `./images/slides/line04-${i + 1}.png`,
    alt: `Line04 이미지 ${i + 1}`,
  })),
};